\
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
...[truncated in markdown preview]...
```

### new_tokens=16384

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the time to get the kernel to do

\































































































 1
















----------------



































































































































































--------




































▲


--
 
 
 
\

\


\



\



\



\



\



\



\




\


\


\


\


\









\



\



\



\



\



\



\



\



\



\



\



\



\



\












































 \




















 \
















 \


\











 \


\













 \
























 \




































 \











 \





 \




 \




 \



 \







 \



 \


 \

\


 \


 \


 \


 \


 \


 \


 \


 \


 \


\


 \


 \


 \


 \


\


 \


 \


\


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \


 \
 \


 \


 \
 \


 \


 \
 \
 \


 \
 \
 \

 \
 \

 \
 \
 \


 \
 \
 \
 \
 \

 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
 \
...[truncated in markdown preview]...
```

## Ratio 0.6

### new_tokens=64

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't aut
```

### new_tokens=128

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodual_dec_of_n_jobs_for_f because it is not autodeamod_of_n_jobs_for_f
 *
 * 3. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 *
```

### new_tokens=256

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodual_dec_of_n_jobs_for_f because it is not autodeamod_of_n_jobs_for_f
 *
 * 3. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 4. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 5. Reducing kernel launch overhead helps autodec_of_n_jobs_for
```

### new_tokens=512

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodual_dec_of_n_jobs_for_f because it is not autodeamod_of_n_jobs_for_f
 *
 * 3. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 4. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 5. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 6. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 7. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 8. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 9. Reducing kernel launch overhead helps autode
...[truncated in markdown preview]...
```

### new_tokens=1024

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodual_dec_of_n_jobs_for_f because it is not autodeamod_of_n_jobs_for_f
 *
 * 3. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 4. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 5. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 6. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 7. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 8. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 9. Reducing kernel launch overhead helps autode
...[truncated in markdown preview]...
```

### new_tokens=2048

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodual_dec_of_n_jobs_for_f because it is not autodeamod_of_n_jobs_for_f
 *
 * 3. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 4. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 5. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 6. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 7. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 8. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 9. Reducing kernel launch overhead helps autode
...[truncated in markdown preview]...
```

### new_tokens=4096

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodual_dec_of_n_jobs_for_f because it is not autodeamod_of_n_jobs_for_f
 *
 * 3. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 4. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 5. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 6. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 7. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 8. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 9. Reducing kernel launch overhead helps autode
...[truncated in markdown preview]...
```

### new_tokens=8192

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodual_dec_of_n_jobs_for_f because it is not autodeamod_of_n_jobs_for_f
 *
 * 3. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 4. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 5. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 6. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 7. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 8. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 9. Reducing kernel launch overhead helps autode
...[truncated in markdown preview]...
```

### new_tokens=16384

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it reduces the number of
 * frames of the of the intermediate-deoder-raw+nulstramem*n*f.
 *
 * 2. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodual_dec_of_n_jobs_for_f because it is not autodeamod_of_n_jobs_for_f
 *
 * 3. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 4. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 5. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 6. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 7. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 8. Reducing kernel launch overhead helps autodec_of_n_jobs_for_f
 * You can't autodec_of_n_jobs_for_f because it is not autodec_of_n_jobs_for_f
 *
 * 9. Reducing kernel launch overhead helps autode
...[truncated in markdown preview]...
```

## Ratio 0.7

### new_tokens=64

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

```

### new_tokens=128

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'
```

### new_tokens=256

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW1
```

### new_tokens=512

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than t
...[truncated in markdown preview]...
```

### new_tokens=1024

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than t
...[truncated in markdown preview]...
```

### new_tokens=2048

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than t
...[truncated in markdown preview]...
```

### new_tokens=4096

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than t
...[truncated in markdown preview]...
```

### new_tokens=8192

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than t
...[truncated in markdown preview]...
```

### new_tokens=16384

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the GPU to do more work per-frame.

Comment: I'm not sure what you mean by "the GPU can do more work per-frame". I'm not an expert in CUDA, but I think the GPU is doing the same work as the CPU is doing.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than the CPU can do.

Comment: @MattW18 I'm not sure either. I'm not an expert either. I'm just saying that the GPU can do more work per-frame than t
...[truncated in markdown preview]...
```

## Ratio 0.8

### new_tokens=64

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames
```

### new_tokens=128

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames. It is based on the observation that the most recent frames in a sequence are most likely to be relevant to the current frame. For example, the current frame is likely to be a face, and the next few frames are likely to be faces in the same pose as the current frame.

The basic idea of
```

### new_tokens=256

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames. It is based on the observation that the most recent frames in a sequence are most likely to be relevant to the current frame. For example, the current frame is likely to be a face, and the next few frames are likely to be faces in the same pose as the current frame.

The basic idea of autoregressive decoding is to decode the current frame using the current model, and then to decode the next frame using the current model and the previous frame. This process is repeated until the decoder is finished.

Autoregressive decoding is a technique that is commonly used in speech recognition. It is also used in video coding. For example, the H.264 video codec uses a technique called the ``reference frame'' mode to reduce the number of bits required to encode a sequence of frames.

Autoregressive decoding is a technique that is commonly used in speech recognition.
```

### new_tokens=512

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames. It is based on the observation that the most recent frames in a sequence are most likely to be relevant to the current frame. For example, the current frame is likely to be a face, and the next few frames are likely to be faces in the same pose as the current frame.

The basic idea of autoregressive decoding is to decode the current frame using the current model, and then to decode the next frame using the current model and the previous frame. This process is repeated until the decoder is finished.

Autoregressive decoding is a technique that is commonly used in speech recognition. It is also used in video coding. For example, the H.264 video codec uses a technique called the ``reference frame'' mode to reduce the number of bits required to encode a sequence of frames.

Autoregressive decoding is a technique that is commonly used in speech recognition. It is also used in video coding. For example, the H.264 video codec uses a te
...[truncated in markdown preview]...
```

### new_tokens=1024

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames. It is based on the observation that the most recent frames in a sequence are most likely to be relevant to the current frame. For example, the current frame is likely to be a face, and the next few frames are likely to be faces in the same pose as the current face.

The basic idea of autoregressive decoding is to decode the current frame using the current model parameters, and then to use the current model parameters to decode the next frame. This process is repeated until the decoder has finished decoding the sequence.

Autoregressive decoding is a technique that is commonly used in speech recognition. It is also used in the context of video coding and video processing. For example, the video codec H.264 uses a technique called motion estimation to predict the motion of pixels in the next frame based on the motion of pixels in the current frame.

Autoregressive decoding is a technique that is commonly used in speech recognition
...[truncated in markdown preview]...
```

### new_tokens=2048

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames. It is based on the observation that the most recent frames in a sequence are most likely to be relevant to the current frame. For example, the current frame is likely to be a face, and the next few frames are likely to be faces in the same pose as the current face.

The basic idea of autoregressive decoding is to decode the current frame using the current model parameters, and then to use the current model parameters to decode the next frame. This process is repeated until the decoder has finished decoding the sequence.

Autoregressive decoding is a technique that is commonly used in speech recognition. It is also used in the context of video coding and video processing. For example, the video codec H.264 uses a technique called motion estimation to predict the motion of pixels in the next frame based on the motion of pixels in the current frame.

Autoregressive decoding is a technique that is commonly used in speech recognition
...[truncated in markdown preview]...
```

### new_tokens=4096

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames. It is based on the observation that the most recent frames in a sequence are most likely to be relevant to the current frame. For example, the current frame is likely to be a face, and the next few frames are likely to be faces in the same pose as the current face.

The basic idea of autoregressive decoding is to decode the current frame using the current model parameters, and then to use the current model parameters to decode the next frame. This process is repeated until the decoder has finished decoding the sequence.

Autoregressive decoding is a technique that is commonly used in speech recognition. It is also used in the context of video coding and video processing. For example, the video codec H.264 uses a technique called motion estimation to predict the motion of pixels in the next frame based on the motion of pixels in the current frame.

Autoregressive decoding is a technique that is commonly used in speech recognition
...[truncated in markdown preview]...
```

### new_tokens=8192

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames. It is based on the observation that the most recent frames in a sequence are most likely to be relevant to the current frame. For example, the current frame is likely to be a face, and the next few frames are likely to be faces in the same pose as the current face.

The basic idea of autoregressive decoding is to decode the current frame using the current model parameters, and then to use the current model parameters to decode the next frame. This process is repeated until the decoder has finished decoding the sequence.

Autoregressive decoding is a technique that is commonly used in speech recognition. It is also used in the context of video coding and video processing. For example, the video codec H.264 uses a technique called motion estimation to predict the motion of pixels in the next frame based on the motion of pixels in the current frame.

Autoregressive decoding is a technique that is commonly used in speech recognition
...[truncated in markdown preview]...
```

### new_tokens=16384

Prompt: `Reducing kernel launch overhead helps autoregressive decoding because`

```text
it allows the kernel to be used for more than just the first frame of the sequence.

\subsection{Autoregressive Decoding}
\label{sec:auto}

Autoregressive decoding is a technique for reducing the number of parameters required to decode a sequence of frames. It is based on the observation that the most recent frames in a sequence are most likely to be relevant to the current frame. For example, the current frame is likely to be a face, and the next few frames are likely to be faces in the same pose as the current face.

The basic idea of autoregressive decoding is to decode the current frame using the current model parameters, and then to use the current model parameters to decode the next frame. This process is repeated until the decoder has finished decoding the sequence.

Autoregressive decoding is a technique that is commonly used in speech recognition. It is also used in the context of video coding and video processing. For example, the video codec H.264 uses a technique called motion estimation to predict the motion of pixels in the next frame based on the motion of pixels in the current frame.

Autoregressive decoding is a technique that is commonly used in speech recognition
...[truncated in markdown preview]...
```

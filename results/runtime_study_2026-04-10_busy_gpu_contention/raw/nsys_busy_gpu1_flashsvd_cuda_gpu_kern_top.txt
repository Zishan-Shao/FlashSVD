Nsight Systems GPU-kernel summary for a short busy-GPU FlashSVD trace.

Caveat:

- this trace includes model load and prefill
- the list below is useful as a qualitative snapshot, not as a steady-state decode ranking

Top visible kernel families:

- BF16 copy / elementwise kernels
- cuBLAS GEMV / GEMM kernels
- CUTLASS GEMM kernels
- FlashAttention forward kernels
- concat / cat copy kernels

Representative top entries from the trace:

- elementwise BF16 copy kernel: `19.4%`
- `gemv2T_kernel_val`: `19.2%`
- `internal::gemvx::kernel`: `7.3%`
- CUTLASS BF16 GEMM kernel: `5.7%`
- FlashAttention split-KV forward kernel: `2.8%`

Key takeaway:

- the busy-card trace still shows the expected FlashSVD decode ingredients
- the slowdown story is not “graph disappeared”; it is that the same GPU work is running in a contended environment
